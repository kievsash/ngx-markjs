import { MarkjsHighlightDirective } from './markjs-highlight.directive';

describe('MarkjsHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkjsHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
